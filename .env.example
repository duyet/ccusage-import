# ClickHouse Connection Configuration
# ==================================
# Copy this file to .env and update with your actual credentials:
#   cp .env.example .env
#   vi .env

# ClickHouse server hostname or IP address
# Examples: localhost, clickhouse.example.com, 192.168.1.100
CH_HOST=your_clickhouse_host

# ClickHouse HTTP/HTTPS port
# Common ports:
#   - 8123: HTTP (default)
#   - 8443: HTTPS
#   - 9440: HTTPS (alternative)
# Note: Protocol auto-detected (HTTPS for ports 443, 8443, 9440)
CH_PORT=8123

# ClickHouse username
# Default: 'default' (ClickHouse's built-in user)
CH_USER=your_username

# ClickHouse password
# Leave empty if no password is configured
# Security tip: Ensure this file has restricted permissions (chmod 600 .env)
CH_PASSWORD=your_password_here

# ClickHouse database name
# Must exist before running the importer
# Default: 'default' (ClickHouse's built-in database)
CH_DATABASE=your_database

# Optional: Custom machine identifier for multi-machine tracking
# ==============================================================
# Default: Uses hostname from socket.gethostname() (e.g., "my-laptop.local")
# Useful for: Distinguishing usage across multiple machines in analytics
# Examples: MACHINE_NAME=work-laptop, MACHINE_NAME=home-desktop
# Uncomment and set to override auto-detection:
# MACHINE_NAME=

# Optional: Disable project privacy protection
# =============================================
# By default, project names and session IDs are hashed to 8-char identifiers
# for privacy protection (e.g., "/Users/me/project" → "a3f4b2c1")
# Uncomment to store original paths (useful for debugging or personal use):
# HASH_PROJECT_NAMES=false