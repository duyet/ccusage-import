//! CLI script entry points used by the binary.
//!
//! Only `import_all` is wired today; other scripts remain scaffold files
//! and are not compiled into the library until they match current APIs.

pub mod import_all;
